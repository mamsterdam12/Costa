# Postgres connection. On Railway this resolves automatically via
# ${{Postgres.DATABASE_URL}} — for local development, point it at your
# own Postgres instance (e.g. `railway run` or a local Docker container).
DATABASE_URL=
